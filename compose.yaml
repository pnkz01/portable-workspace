services:
  container_bass:
    build: .
    hostname: dubiandubaian
    volumes:
      - dubian_data:/home/dubian
    ports:
      - "443:443"
    privileged: true
    stdin_open: true
    tty: true

volumes:
  dubian_data:
